# nodejs-ssr-ssg-rendering-simulation
This example demonstrates Server-Side Rendering (SSR) and Static Site Generation (SSG) concepts, similar to how Next.js handles them. It uses a single Node.js script to simulate a web server generating dynamic content per request (SSR) and pre-generating a static HTML file (SSG). The output HTML includes a timestamp and random number to highlight t
